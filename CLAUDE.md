# CLAUDE.md

보드게임 **푸에르토리코 1897**을 소개하고, 브라우저에서 바로 플레이할 수 있게 만든 **비공식 팬 프로젝트**입니다.
빌드 도구·서버·패키지 매니저 없이 **정적 파일만으로 동작**하는 것이 핵심 제약입니다 —
`game.html`을 더블클릭(file://)해도 그대로 실행돼야 하고, 그래서 ES 모듈·CDN·번들러를 쓰지 않습니다.
(2026-08 이전에는 게임 전체가 HTML 한 파일이었으나, 유지보수를 위해 역할별 css/·js/ 파일로 분리했습니다.)

## 프로젝트의 두 가지 목표

1. **소개** — 게임을 모르는 사람이 규칙 설명 페이지만 읽고도 "해볼 만하겠다"고 느끼게 한다.
2. **플레이** — 지금은 AI(봇) 상대로 혼자, 나중에는 Supabase 기반 온라인 멀티플레이로.

두 목표 모두 **UI/UX 친화성이 최우선 판단 기준**입니다. 기능이 하나 더 늘어나는 것보다,
처음 보는 사람이 헤매지 않는 쪽을 택합니다. (아래 "UI/UX 원칙" 참고)

## 파일 구조

```
index.html                      ← 규칙 설명 페이지 (소개/방법/상품/건물/종료 + 플레이 탭). GitHub Pages 진입점.
                                   본문(콘텐츠 HTML)만 남기고 스타일·스크립트는 아래 intro 파일로 분리됨.
css/intro.css                   ← 소개 페이지 스타일
js/intro.js                     ← 소개 페이지 스크립트 — 탭 전환·역할/건물 목록 데이터와 그리기
game.html                       ← 게임 본체 셸 — css/·js/ 를 순서대로 로드하는 얇은 HTML. 코드는 아래 파일들에 있다.
css/game.css                    ← 게임 스타일 전체
js/error-guard.js               ← 전역 오류 안전망 (반드시 첫 번째로 로드)
js/data.js                      ← 게임 데이터 상수 (GOODS·BUILDINGS·ROLES·BORDER·PCOLOR·imgTag)
js/state.js                     ← 게임 상태 G · 게임 생성 (newGame·playerCountSetup·shuffle) · 조회 유틸 (P·log·occB…)
js/view.js                      ← 뷰 상태(uiXxx 전역)와 연출 — 토스트·이펙트·패널 가시성·보드 폭·키보드
js/engine.js                    ← 규칙 엔진 — 라운드/역할 선택·6단계 진행(xxxNext/actXxx)·게임 종료/점수
js/ai.js                        ← 봇 — aiDecide와 평가 함수(aiValueOfRole·aiBuildScore)
js/storage.js                   ← localStorage 저장/복구 (lsGet/lsSet 래퍼 포함)
js/render.js                    ← 렌더링 — 부분 렌더 인프라(setPart)·설정 화면·render()
js/panel.js                     ← 중앙 액션 패널(renderActionPanel)·하단 액션바(renderActionBar)
js/ui.js                        ← 사람 전용 UI 헬퍼 (uiToggleLand·uiStoreDone…)
js/net-config.js                ← Supabase 접속 설정 (publishable 키 — 공개 전제, RLS가 접근 제어)
supabase/                       ← 전용 Supabase 프로젝트 스키마 (setup.sql·migrations/·config.toml)
.mcp.json                       ← Supabase MCP 연결 (Claude Code에서 /mcp 로 인증 후 사용)
js/net.js                       ← 온라인 멀티플레이 — 방/좌석/intent/snapshot/연출 중계/채팅 (아래 "온라인" 섹션)
js/vendor/supabase.js           ← supabase-js UMD 번들 (CDN 금지 원칙 — 저장소에 포함)
js/app.js                       ← 흐름 제어(schedule)와 부팅
tools/smoke.js                  ← 헤드리스 스모크 테스트 (node tools/smoke.js)
tools/nettest.js                ← 온라인 루프백 테스트 — 가짜 Supabase 버스로 호스트+게스트 완주 (node tools/nettest.js)
.github/workflows/deploy.yml    ← main 푸시 시 GitHub Pages 자동 배포
푸에르토리코1897_웹규칙서.pdf       ← 정식 규칙서 발췌 (이미지 원본 출처)
img/                            ← 역할·건물·농장 타일 이미지
```

**`index.html`이 규칙 설명 페이지의 소스입니다.** 예전에는 9.8MB 번들러 산출물
`푸에르토리코 1897 (오프라인).html`(내용 대부분이 임베드된 웹폰트)이 함께 있었고 index.html이
그것을 손으로 편집 가능한 형태로 재구현한 것인데, 참고용으로만 남아 있던 그 번들과 백업·스크린샷은
2026-08-09에 저장소에서 삭제했습니다 (필요하면 그 이전 커밋에서 꺼낼 수 있습니다).
소개 페이지의 "플레이" 탭은 게임 본체(game.html)를 iframe으로 띄웁니다 (처음 열 때 지연 로드).
규칙 설명 본문 HTML은 index.html 안에 그대로 둡니다 — file:// 에서는 fetch가 막혀
본문을 외부 파일로 빼려면 JS 문자열로 만들어야 하는데, 그러면 콘텐츠 편집성이 나빠집니다.

**js/ 파일 분리 원칙** — 각 파일은 한 가지 책임만 가진다 (데이터 / 상태 / 뷰 연출 / 규칙 / AI / 저장 / 렌더).
- 새 코드는 그 책임에 맞는 파일에 넣는다. 애매하면: 규칙이 바뀌면 engine, 화면만 바뀌면 render/panel, 봇 판단이면 ai.
- **일반 `<script>` 태그를 유지한다** (`type="module"` 금지 — file:// 에서 CORS로 죽는다).
  최상위 `let/const/function`은 스크립트 간 전역으로 공유되므로 import/export 없이 서로를 호출한다.
- 로드 순서는 game.html에 적힌 의존 방향(데이터 → 상태 → 뷰 → 엔진 → AI → 저장 → 렌더 → UI → 부팅)을 따른다.
  같은 이름의 최상위 `let/const`를 두 파일에 선언하면 두 번째 파일이 로드 시점에 죽는다 — 전역 이름은 프로젝트 전체에서 유일해야 한다.

## 실행 / 확인

빌드도 서버도 없습니다. `game.html` 을 브라우저에서 바로 엽니다 (file:// 그대로 동작).
새 게임으로 다시 시작하려면 헤더의 "새 게임" 버튼(또는 `localStorage.removeItem('pr1897_save')`).

`node tools/smoke.js` — AI 4인 게임을 헤드리스로 완주시키는 스모크 테스트. 파일 분리로 인한
참조 누락·로드 순서 문제와 규칙 엔진의 예외를 잡습니다 (DOM은 스텁이라 화면은 검증 못 함).
`node tools/nettest.js` — 온라인 루프백 테스트. 가짜 Supabase 버스로 호스트·게스트 두 클라이언트를
띄워 2인 온라인 게임을 완주시킵니다 (대기실 흐름·intent 검증·덱 가림·채팅·점수 일치 검증).
실제 Supabase 채널은 여기서 검증되지 않으므로, 전송 계층을 건드렸다면 브라우저 두 개로도 확인할 것.
규칙 로직을 건드렸다면 스모크 테스트 후 **실제로도 한 판 돌려보고** 확인합니다.
헤더의 "파일 저장 시각" 표시는 game.html(셸)의 수정 시각이라, js만 고치면 갱신되지 않습니다.

## 배포 (GitHub Pages)

**main 브랜치에 푸시하면 자동 배포됩니다** — `.github/workflows/deploy.yml`이
사이트에 필요한 파일(index.html·game.html·css/·js/·img/)만 모아 GitHub Pages로 올립니다.
구버전 번들·PDF·스크린샷은 배포에 포함하지 않습니다 (용량 절약 + 저작권 자료 노출 최소화).

- 배포 주소: https://krindale.github.io/puerto-rico-1897-web/
- 진입점은 index.html(소개 페이지)이고, 게임은 그 안의 "플레이" 탭 또는 `/game.html` 직접 접근.
- 새 정적 자원(폰트·이미지 폴더 등)을 추가하면 deploy.yml의 "사이트 파일만 모으기" 단계에도 추가할 것.
- 이 프로젝트는 비공식 팬 프로젝트입니다 — 배포물에 광고·유료화 요소를 넣지 마세요 (저작권 주의 참고).

## 아키텍처

게임 코드는 js/ 아래 역할별 파일로 나뉘어 있고(위 "파일 구조" 참고), 일반 `<script>` 전역 공유로 연결됩니다.
읽는 순서는 로드 순서와 같습니다: 상수 → 상태 → 뷰 → 게임 로직 → AI → 저장 → 렌더.

### 상태

전역 변수 `G` 하나가 게임 전체 상태입니다. 순수 JSON(함수·순환참조 없음)이라
`JSON.stringify(G)` 로 통째로 `localStorage['pr1897_save']` 에 저장됩니다. **이 직렬화 가능성을 깨지 마세요** —
온라인화의 전제 조건입니다.

```js
G = {
  n, round, governor, over, endReasons, scores,
  players: [{ i, name, ai, coins, vp, goods{5종}, land[{type,w}], buildings[{id,w}], stored }],
  supply:  { workers, vp, goods, quarries, deck, discard, display, stock,
             ships[{size,type,count}], labor, market },
  roles:   [{ id, coins, takenBy }],
  phase, pending, log,
}
```

### 진행 프로토콜 — `phase` / `pending`

게임 흐름의 중심입니다. 새 규칙·새 상호작용을 추가할 때 반드시 이 패턴을 따르세요.

- `G.phase` — 지금 진행 중인 역할 단계 (`startPhase(roleId, chooser)` 로 시작)
- `G.pending` — **지금 누가 무엇을 결정해야 하는가**. 이게 없으면 게임은 멈춰 있는 상태.

`pending.type` 목록: `pickRole` · `settler` · `mayorPlace` · `builder` · `craftBonus` ·
`trader` · `captain` · `storage` · `report` · `phaseEnd` · `gameOver`

`phaseEnd`는 **단계 마무리 일시정지**입니다 — 각 단계의 마지막 행동 직후 1초(`PHASE_END_HOLD`, app.js)
멈췄다가 `actPhaseEnd()`가 실제 마무리(`xxxFinish()`: 결과 보고·다음 차례)를 실행합니다.
마지막 행동과 결과 창이 같은 프레임에 겹쳐 "무슨 일이 있었는지 안 보이는" 문제를 막기 위한 것으로,
pending이므로 직렬화되고 새로고침해도 `schedule()`이 타이머를 다시 겁니다.
새 단계를 추가하면 끝 지점에서 `phasePause(id)`를 부르고 `actPhaseEnd()`에 분기를 추가하세요.

각 단계는 `xxxNext()` (다음 차례 사람에게 `pending` 을 넘기거나, 다 돌았으면 `xxxEnd()`)와
`actXxx()` (사람/AI 양쪽이 호출하는 **유일한** 행동 진입점) 쌍으로 되어 있습니다.
행동을 처리한 뒤에는 `schedule()` 을 부릅니다.

`schedule()` = `render()` + `save()` + (현재 차례가 AI면 420ms 뒤 `aiDecide()`).
**모든 상태 변경은 `schedule()` 로 끝나야 합니다.** 여기 안 거치면 저장이 안 되고 AI가 멈춥니다.

### 렌더링

가상 DOM 없음. `render()` 는 매번 모든 섹션의 HTML **문자열**을 새로 만들지만, DOM에는
**문자열이 지난번과 달라진 섹션만** 써넣습니다 (`setPart(id, html)` — 내부에 문자열 캐시).
스켈레톤(고정 컨테이너 `#rd-hdr` `#rd-roles` `#rd-players` `#rd-plants` … `#rd-bar` `#rd-modal` `#rd-panel` `#rd-shop`)은
게임 시작 시 한 번만 세워집니다. 플레이어 보드는 `setBoards()`가 바뀐 보드만 노드 교체.
이 덕분에 AI 턴처럼 렌더가 잦아도 진행 중인 CSS 애니메이션이 안 끊기고 화면이 번쩍이지 않습니다.
**새 섹션을 추가하면 스켈레톤에 컨테이너를 만들고 `setPart`로 채우세요.** `$app.innerHTML`을 직접
다시 쓰는 곳은 설정 화면(`renderSetup`, 진입 시 `rdReset()` 필수)뿐입니다.

이벤트는 인라인 `onclick` (`uiToggleLand(0,2)` 같은 전역 함수 호출). 새 UI를 추가할 때도
이 방식을 유지하세요 — 일관성이 없으면 innerHTML 교체 때 리스너가 날아갑니다.

사람 전용 UI 헬퍼는 `ui` 접두사 (`uiToggleLand`, `uiMayorAuto`, `uiStoreDone`…),
선택 상태는 전역 `uiSel` 에 임시로 담고 확정 시 `actXxx()` 로 넘깁니다.

**개인 보드 폭의 우선순위** — 한 줄에 [펼친 보드 1개 + 접힌 요약 카드 (인원−1)장]이 들어가야 합니다.
펼친 보드가 필요로 하는 폭(건설 부지 6칸 = 타일 6개 + 간격)이 우선이고, 접힌 카드는 `CB_MIN`(84px)만
가져갑니다. 그래도 모자라면 `render()` 가 `#rd-players` 에 `--bw`(건설 부지 타일)·`--lw`(토지 타일)를
더 작은 값으로 얹어 **잘라내는 대신 타일을 줄입니다** (`boardTileW()`). 5인·최대 창에서 타일은 115px,
1360px 창이면 99px입니다. 요약 카드에 새 정보를 넣을 때는 **84px 폭에서 안 깨지는지** 확인하세요 —
이름 목록처럼 폭을 요구하는 것은 접힌 카드에 넣지 않습니다 (툴팁이나 펼친 보드로).

**사람 차례의 선택지는 중앙 액션 패널로 보여줍니다.** `renderActionPanel()`이 개척·모집·생산 보너스·판매·선적·저장
(`PANEL_TYPES`)의 pending을 건물 보관판과 같은 중앙 팝업으로 그립니다 — 선택지·불가 사유·승점/주화 미리보기,
그리고 `G.phase.hist`(이번 단계에 누가 무엇을 했는지, 각 `actXxx()`가 `phaseHist()`로 기록)를 함께 표시합니다.
새 상호작용을 추가하면 ① `PANEL_TYPES`에 등록하고 패널 케이스를 만들고 ② 액션바 안내도 같이 추가하세요.
패널 열림 상태는 `uiPanelOpen`(null=자동)이며 `uiShopOpen`과 같은 규칙(홀드 중 숨김, pending 전환 시 자동 모드 리셋)을 따릅니다.
단, **선적·판매 단계는 봇 차례에도 패널이 단계 내내 유지**되어 전원의 상품·수송선/상점 진행을 그 안에서 보여주고
(개인 보드 아코디언은 이 두 단계 동안 움직이지 않음 — `shownBoard()` 참고), 같은 단계 안에서는 열림 상태를 리셋하지 않습니다.
생산·판매·선적 단계가 끝나면 결과를 `phaseReport()`로 보여주는데, 사람이 있으면 **[확인]을 누를 때까지 게임이 멈추는
pending(`type:'report'`, 직렬화 가능 — 새로고침 복구됨)**이고 봇만 있으면 지나가는 패널(`phaseFlash`)입니다.

**뷰 상태는 `G`에 넣지 않습니다.** 어떤 보드를 펼쳤는지(`uiBoardSel`), 건물 보관판 팝업이 열렸는지(`uiShopOpen`) 같은
화면 상태는 전역 변수로만 관리합니다 — 저장·되돌리기·(추후) 온라인 동기화 대상이 아니기 때문입니다.
`null`은 "자동" 모드(현재 차례를 따라감 / 내 건설 차례에 팝업 자동 오픈)이며, `render()` 상단에서 pending 전환을
감지해 자동 모드로 리셋합니다.

**뷰 상태는 새로고침하면 사라진다는 걸 렌더가 전제해야 합니다.** `G`만 `localStorage`에서 복구되고 뷰 전역은
초기값으로 돌아가므로, 저장된 `pending`을 그리는 코드가 뷰 전역이 채워져 있다고 가정하면 **렌더가 통째로 죽어
화면에 배경만 남습니다**. 실제로 `uiSel`(저장 단계 선택)이 그랬습니다 — 저장 단계에서 새로고침하면
`uiSel.types`에서 예외. 지금은 `storeSel()`이 없으면 만들어 씁니다. 새 뷰 전역을 추가할 때도 같은 방식으로
**읽는 쪽에서 복구**하세요. 본문 스크립트보다 먼저 도는 `window.onerror` 안전망이 이런 사고를 화면에
띄워 주지만, 그건 어디까지나 최후의 그물입니다. 개인 보드는 아코디언(한 명만 펼침, ←/→ 키·클릭 전환), 건물 보관판은 팝업입니다.

### AI (봇)

`aiDecide()` 가 `pending.type` 별로 분기해 `actXxx()` 를 호출합니다.
평가 함수는 `aiValueOfRole()`, `aiBuildScore()` — 휴리스틱 점수제이며 동점 시 `Math.random()*0.15` 로 흔듭니다.
**AI는 사람과 똑같은 `actXxx()` 진입점만 씁니다.** 이 규칙을 지켜야 규칙 변경이 양쪽에 자동 반영됩니다.

### 데이터 상수

- `ROLES` — 역할 7종 (탐험가는 4~5인용으로 2장, `prospector`/`prospector2`)
- `BUILDINGS` — 건물 23종 (생산 6 / 상업 12 / 고급 5, 고급은 `size:2`)
- `BORDER` — 건물 진열 순서
- `playerCountSetup(n)` — 2~5인별 초기 주화·일꾼·승점칩·수송선·인력시장·제거할 농장 수. **2인 특별 규칙이 여기 들어 있습니다.**

### 타일 이미지

`imgTag(cat, nm)` 이 `img/{역할|건물|농장}_{한글이름}.png` 를 만듭니다 (**띄어쓰기 없이**).
파일이 없으면 텍스트 대체 표시로 자동 폴백하므로, 이미지가 없다고 깨지지 않습니다.

## 코딩 컨벤션

- **한국어 우선** — UI 문구, 로그, 주석 모두 한국어. 변수·함수명은 영어.
- 빌드가 없으므로 **의존성 추가 금지**. CDN 스크립트도 넣지 마세요 (오프라인 실행이 요구사항).
- 섹션 구분은 기존 `/* ═══ 섹션명 ═══ */` 배너를 따릅니다.
- 규칙 관련 로직에는 **왜 그런지**를 한 줄 주석으로 남깁니다 (예: `// 병원: 공급처 → 인력시장 → 없음`).
  원작 규칙의 예외 조항이 많아서, 나중에 보면 버그처럼 보이는 코드가 대부분 의도된 규칙입니다.
- 커밋 메시지는 한국어.

## 작업 규칙 (필독)

**[docs/작업-실수-기록.md](docs/작업-실수-기록.md)** — 실제로 지적받은 실수들과 재발 방지 규칙.
작업 전에 읽고 지킬 것. 요점:

- **Playwright(헤드리스 브라우저) 사용 금지** — 화면 확인이 필요하면 사용자에게 스크린샷을 요청한다.
- 시각적 변경은 "됐다" 단정 금지 — 계측 근거로 보고하고 최종 판단은 사용자 확인에 맡긴다.
- 하나를 고치기 전에 같은 패턴을 grep으로 전수 조사해 한 번에 고친다.
- 변경마다 커밋·푸시 금지 — 사용자 확인/요청 시 의미 단위로만.
- 새 함수·클래스는 정의–호출/사용 짝을 확인하고, 연관 상수·주석을 함께 갱신한다.

## 코드 리뷰 방식

**코드 리뷰 시 서브 에이전트를 한 번에 여러 개 띄우지 마세요. 순차적으로 하나씩 실행합니다.**
관점(정확성·단순화·성능 등)을 나눠 보더라도 병렬 팬아웃이 아니라 앞 리뷰가 끝난 뒤
다음 리뷰를 시작하는 방식으로 차례차례 진행합니다.

## UI/UX 원칙

이 프로젝트의 사용자는 **규칙을 모르는 사람**이 기본값입니다. 아래를 판단 기준으로 삼으세요.

0. **스크롤 없이 한 화면.** 게임 화면 전체가 세로 스크롤 없이 한눈에 들어와야 합니다 (사용자가 명시한 최우선 원칙).
   패널을 추가할 때는 다른 것을 압축하거나 팝업으로 빼서 총 높이를 지키세요.
   사이드 컬럼도 내부 스크롤이 생기면 안 됩니다 (로그만 예외).
1. **지금 뭘 해야 하는지 항상 화면에 있어야 한다.** 하단 액션바(`renderActionBar`)가 그 역할을 합니다.
   새 상호작용을 추가하면 액션바 안내 문구도 반드시 같이 추가하세요.
2. **불가능한 선택지는 비활성이 아니라 이유를 보여준다.** 건물을 못 짓는다면 "주화 부족" / "부지 없음" 중 뭔지 보이게.
3. **되돌릴 수 있게 한다.** 일꾼 배치처럼 여러 번 만지는 조작은 확정 버튼 전까지 자유롭게 토글 가능해야 합니다 (현재 `uiToggleLand`/`uiToggleBld` 방식).
4. **`confirm()` / `alert()` 을 늘리지 않는다.** 현재 "새 게임" 한 곳에만 있습니다. 나머지는 화면 안에서 해결.
5. **모바일에서 깨지지 않게.** 1000px 이하에서 2단 레이아웃이 1단으로 접힙니다. 새 패널도 같은 브레이크포인트를 따르세요.
   좁은 화면(1단)의 규칙은 아래 "모바일 레이아웃"을 따르세요 — 특히 **새로 넣는 좁은 화면용 CSS는 반드시 파일 끝**에 둡니다.
6. **색만으로 정보를 전달하지 않는다.** 상품 5종은 색+글자(`goodChip`)를 같이 씁니다.
7. 서체·색은 `:root` CSS 변수(`--bg --ink --line --panel --accent --dim --gold`)만 사용. 하드코딩된 색을 새로 만들지 마세요.

## 모바일 레이아웃 (1000px 이하)

390px(아이폰 세로)에서 한 판을 끝까지 돌 수 있게 맞춰져 있습니다. 손대기 전에 아래를 읽으세요.

**타일 축소가 잘림을 막는다.** `boardMetrics()`(view.js)에 1단 전용 분기가 있습니다 — 컨테이너 폭에 맞춰
건설 부지·토지 타일을 줄여(`BW_MIN_NARROW`=40px) 6칸이 항상 다 보이게 합니다. 390px에서 타일은 51px입니다.
`tools/smoke.js`의 **보드 폭 회귀 테스트**가 1단 6개 폭 × 2~5인을 검사하니, 이 계산을 바꾸면 그것부터 돌리세요.

**개인 보드는 가로 스크롤 스트립입니다.** 펼친 보드가 화면 폭을 통째로 쓰고 접힌 카드가 그 오른쪽으로 이어집니다.
펼친 보드가 바뀌면 `render()`가 그 보드로 **즉시** 스크롤합니다 — `behavior:'smooth'`를 쓰면 직후의 폭 변경 렌더가
애니메이션을 취소해 스크롤이 통째로 무시됩니다. `scroll-snap`도 쓰지 않습니다(폭이 바뀔 때 되돌려 버립니다).

**공용 정보는 역할 타일 바로 아래 접이식 두 줄**(`.mfold`)입니다 — 공용 보드(공개 농장·수송선·상점·인력시장·공급처)와 기록.
접힌 줄에도 요약 수치(인력·배·상점)를 얹어 펼치지 않고도 판단이 끝나게 합니다. 펼침 상태(`uiFold`)는 기억하지 않고
pending이 바뀌면 접힙니다. DOM은 데스크톱과 같고, `.main-col{display:contents}` + `order`로 순서만 바꿉니다
(이때 `min-width:0`을 다시 걸어야 합니다 — 안 그러면 페이지에 가로 스크롤이 생깁니다).

**터치에는 호버가 없습니다.** `tapPreviewMode()`(= `(hover:none)`)가 참이면 건물·역할 카드의 첫 탭은 확정이 아니라
**미리보기 시트**(`uiPreview`)를 엽니다 — 확대 이미지·효과·"이 선택을 하면 어떻게 되는지"(주화/부지)를 보여주고
확정은 시트의 버튼에서만 일어납니다. 데스크톱은 이 경로를 타지 않아 호버 확대가 그대로입니다.

**하단 액션바는 fixed라 자리를 차지하지 않습니다.** `.wrap`의 아래 여백(112px)이 그 자리입니다 —
단축 `padding`으로 덮어쓰지 마세요(실제로 640px 블록이 덮어써서 폰에서만 보드가 가렸습니다).

**좁은 화면용 CSS는 파일 끝에.** `.overlay`·`.modal`·`.ap-rolecell` 같은 기본 규칙이 파일 뒤쪽에 선언돼 있어서,
미디어 쿼리를 위에 두면 같은 특정도의 기본 규칙이 나중에 나와 **조용히 덮어씁니다**. 특정도가 다른 경우도
조심하세요 — `.pboard.board-in.cardflash`(0,3,0)가 미디어 쿼리 안의 `.pboard.board-in`(0,2,0)을 이겨서
모바일에서 보드 폭 애니메이션이 되살아났던 적이 있습니다.

## 앞으로 만들 것

### A. 지금 구조에서 바로 할 수 있는 것 (온라인화 전)

**초보자 온보딩 — 최우선**
- [ ] 첫 플레이 시 단계별 가이드 (역할 선택 → 각 단계가 무엇인지 1회성 툴팁)
- [x] 각 역할/건물 카드에 **호버·탭 시 효과 설명** — 터치에서는 탭 미리보기 시트(`uiPreview`)로 노출됨.
      데스크톱은 여전히 호버 확대뿐이라 `fx` 문구가 안 보인다 (남은 몫)
- [x] "이 선택을 하면 어떻게 되는지" 미리보기 — 탭 미리보기 시트가 남는 주화·부지를 보여준다 (터치 한정)
- [ ] 규칙 설명 페이지 ↔ 게임 간 상호 링크 (모르는 용어에서 규칙으로 점프)

**알려진 버그**
- ~~봇의 일꾼 배치가 화면에서 보이지 않음~~ — **수정 완료.** 원인이 여러 개 겹쳐 있었다:
  1. 봇 행동 직후 사람 차례로 pending이 넘어가면 `render()`가 같은 프레임에서 `uiBoardSel`/`uiShopOpen`을
     즉시 리셋해 방금 벌어진 일이 화면에 뜰 틈이 없었음 → `fxMark`가 살아있는 동안(`FX_HOLD`=1.5초, 최상단
     상수) 사람 차례 전환(보드 스냅 + 팝업 오픈)을 `setTimeout`으로 미루도록 수정.
  2. `mayorAutoPlace`가 "새로 놓인 자리가 있을 때만" 그 플레이어 보드로 전환했는데, 인력시장이 말라서
     재배치 결과가 이전과 동일한(=새로 놓인 자리 0개) 봇 차례는 보드 전환 자체가 생략돼 통째로 스킵된
     것처럼 보였음 → 새로 놓인 자리가 없어도 `markFxKeys`를 항상 호출해 보드는 항상 전환되게 수정.
  3. 개인 보드 아코디언 전환(넓어지는 레이아웃)과 일꾼 토큰의 작은 팝 애니메이션이 동시에 일어나면
     토큰 팝은 눈에 안 띔 → `.pboard`에 `cardFxCls('pboard-'+p.i)`로 카드 전체 골드 테두리 글로우
     (`.cardflash`, 1.3초)를 추가해 "보드가 전환됐다" 자체를 확실히 보이게 함.
  4. 라운드 마지막 봇의 배치 직후 "라운드 N 시작" 같은 큰 토스트가 같은 프레임에 겹쳐 떠서 방금 전
     플레이어의 이펙트를 가려버림 → `toast()`에서 `big` 토스트는 `fxMark`가 아직 살아있으면(`FX_HOLD`)
     그만큼 지연 발사하도록 수정.
  5. (2)~(4)로도 "마지막 배치자 다음이 사람 차례"인 경우는 여전히 문제였음 — `uiBoardSel`/`uiShopOpen`은
     지연시켰지만, 역할 타일 `pickable` 표시·액션바(`renderActionBar`)·건설/개척/모집 각 `*Active` 플래그는
     `pd`/`curPi`를 직접 보고 그 자리에서 즉시 그려져서 "당신 차례" UI가 먼저 떠버렸음 → `uiHoldUntil`
     타임스탬프를 두고 `render()` 맨 위에서 `holding=Date.now()<uiHoldUntil`을 계산, 저 다섯 곳
     모두 `!holding` 조건을 추가해 지연 구간 동안은 "당신 차례" 티가 전혀 안 나게 통일함.
  새 UI를 "사람 차례에만 보여야 하는 것"으로 추가할 때는 `pd.type`/`curPi`만 보고 판단하지 말고
  `holding` 도 반드시 같이 체크할 것 — 안 그러면 이 버그가 그 UI에서 재발한다.

**플레이 편의**
- ~~되돌리기(Undo)~~ — 구현했다가 **사용자 결정으로 제거** (게임성을 떨어뜨린다는 판단). 다시 제안하지 마세요.
- [ ] 게임 로그를 접었다 펼 수 있게 + 방금 일어난 일 하이라이트
- [ ] 최종 점수 화면의 **점수 내역 분해** (승점칩 / 건물 / 고급건물 보너스)
- [ ] 난이도 선택 (AI 휴리스틱 강도)
- [ ] 키보드 조작 · 접근성 (탭 이동, `aria-label`)

**소개 페이지**
- [ ] "3분 요약" 섹션 — 승리 조건과 한 라운드 흐름만
- [ ] 스크린샷 기반 인터랙티브 예시 한 판

### B. 온라인 멀티플레이 — 구현됨 (2026-08, js/net.js)

**이 게임 전용 Supabase 프로젝트를 씁니다** — `puerto-rico-1897` (ref `cpdvwgwxkqhcsqfxzdec`, 서울),
aos_showcase 프로젝트와 완전히 분리(사용자 결정). 스키마·RLS·정리 크론은 쇼케이스에서 검증된 것을
가져와 `supabase/setup.sql`(대시보드 붙여넣기용) + `supabase/migrations/`(CLI `db push`용)에 둡니다.
전제 설정: Authentication → Anonymous sign-ins ON (config.toml `enable_anonymous_sign_ins=true` →
`supabase config push`로 적용됨). DB 비밀번호는 `.env.local`(git 미추적).
realtime.messages 정책은 프로젝트 생성 직후엔 적용이 안 된다(Realtime 서비스가 떠야 테이블이 생김) —
그래서 별도 마이그레이션(...realtime_policies.sql)이며, 실패하면 잠시 후 `db push` 재시도.

**모델: 호스트 권위(host-authoritative).** 서버(Edge Function)가 아니라 방을 만든 클라이언트가 권위입니다.

- **호스트**: 엔진·AI를 전부 돌린다. `save()`가 매 상태 변화마다 `netPushState()` →
  snapshot 브로드캐스트 + (1초 디바운스) DB `rooms.snapshot` 저장(재접속용).
- **게스트**: 엔진을 돌리지 않는다. `netWrapActs()`가 행동 진입점(`actXxx`·`pickRole`)을
  intent 전송으로 감싸고, snapshot을 받아 `G`를 통째로 교체 후 render.
  일꾼 배치만 예외 — 로컬에서 자유롭게 토글한 결과를 통째로 보내고 호스트가 총량을 검증.
- **intent 방어**: 호스트는 게스트가 보낸 값을 전부 의심한다 — 멱등성 id로 중복 제거,
  `netValidIntent()`로 현재 pending 기준 유효성 검증, try/catch로 예외 격리.
  (루프백 테스트가 실제로 잡은 사고: 중복 intent가 낡은 인자로 호스트 엔진을 죽였음)
- **비공개 정보**: 게스트에게 가는 snapshot은 `netStripDeck()`이 농장 덱을 `'?'`로 가린다(길이만 보임).
  한계(수용): DB의 `rooms.snapshot`은 완전한 상태라, 참가자가 REST로 직접 읽으면 덱이 보인다 —
  호스트 재접속·복구에 완전한 상태가 필요해서 남긴 절충이다.
- **연출 중계**: 호스트의 `toast`/`phaseFlash`/`markFxKeys`/`markPanelFx`/`roleSplash` 호출을
  `netHookFx()`가 fx 이벤트로 중계 → 게스트도 같은 연출을 본다.
- **차례 판정**: `isLocalHuman(pi)` (view.js) — 로컬이면 사람 전부, 온라인이면 내 좌석만.
  "당신 차례" UI는 전부 이걸 봐야 한다. `!p.ai`만 보면 남의 원격 차례가 내 화면에서 조작된다.
- **이탈 대응**: presence로 끊김을 감지, 끊긴 사람 차례에 호스트 액션바에 [AI가 이어받기] 버튼.
  호스트 승계는 없다(호스트가 나가면 게임 중단) — 호스트는 "온라인 게임 이어서"로 재접속 가능.
- **채팅**: broadcast 이벤트(휘발성, DB 저장 안 함) + 플로팅 위젯(net.js가 body에 직접 그림 —
  게임 렌더와 무관해 입력이 안 끊긴다). 안읽음 배지 + Web Audio 딩동.

**하지 않은 것 (다음 단계 후보):** 서버 권위 검증(Edge Function) · 시드 PRNG · 액션 객체 + 순수
리듀서 · 엔진/렌더 분리 · 호스트 승계 · 턴 제한 시간 · 관전 · 리플레이 · 전적.
서버 권위로 가려면 액션 객체 리팩터링이 선행돼야 한다 (pending 프로토콜이 이미 그 모양이라 전환 비용은 작다).

**로컬 플레이는 계속 동작해야 합니다.** 온라인은 추가 모드이지, 대체가 아닙니다.
game.html을 더블클릭으로 열어 AI와 한 판 하는 경험은 이 프로젝트의 정체성입니다.
vendor 번들이 없거나 네트워크가 막혀도 `netConfigured()`가 false가 되어 온라인 UI만 숨습니다.
온라인 게임은 로컬 저장 키(`pr1897_save`)를 건드리지 않습니다 — 혼자 하던 게임이 보존됩니다.

## 저작권 주의

- Ravensburger / 코리아보드게임즈와 무관한 비공식 팬 프로젝트입니다.
- `img/` 의 타일 일러스트는 **정식 규칙서에서 발췌한 원작사 저작물**입니다.
  상업적 이용, 광고 삽입, 유료화 관련 코드를 제안하거나 추가하지 마세요.
- 배포 관련 작업을 요청받으면 이 점을 먼저 짚어주세요.
